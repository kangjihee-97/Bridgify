<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.cloud.mapper.PriceHistoryMapper">

    <select id="findPrice" resultType="java.math.BigDecimal">
        SELECT price
        FROM price_history
        WHERE ticker = #{ticker}
          AND `year` = #{year}
    </select>

    <select id="countByTicker" resultType="int">
        SELECT COUNT(*)
        FROM price_history
        WHERE ticker = #{ticker}
    </select>

    <insert id="upsert">
        INSERT INTO price_history (ticker, `year`, price)
        VALUES (#{ticker}, #{year}, #{price})
        ON DUPLICATE KEY UPDATE price = VALUES(price)
    </insert>

</mapper>